#include <iostream>
#include <cstdio>

using namespace std;

int main(){

 int arr[] = {1, 2, 3, 4, 5, 6, 7, 8};
 int size = sizeof(arr)/sizeof(arr[0]);

 cout<<"The number of elements in the array is :"<<size;

 return 0;
}
